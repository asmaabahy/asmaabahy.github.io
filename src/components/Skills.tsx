import SectionTitle from "./UI/SectionTitle";

export default function Skills() {
  return (
    <section
      id="skills"
      className="h-[70vh] bg-marian-blue p-5 flex flex-col justify-center items-center"
    >
      <SectionTitle label="Skills" className="text-black bg-yellow" />
    </section>
  );
}
